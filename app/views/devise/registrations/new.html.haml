= render "registration_header"

.contents-2
  .contents__message2
    新規会員登録
  = form_with url: "#", local: true  do |f|
    .main-form
      １．ユーザー情報
      .form-group
        = f.label :nickname, "ニックネーム"
        %span.form-require
          必須
        = f.text_field :nickname, placeholder: "例) フリマ太郎", class: "input-default"
      .form-group
        = f.label :email, "メールアドレス"
        %span.form-require
          必須
        = f.email_field :email, placeholder: 'PC・携帯どちらでも可', class: 'input-default'
      .form-group
        = f.label :password, "パスワード"
        %span.form-require
          必須
        = f.password_field :password, placeholder: '7文字以上', class: 'input-default'   
      .form-group 
        = f.label :password2, "パスワード(確認)"
        %span.form-require
          必須
        = f.password_field :password_confirmation, placeholder: '7文字以上', class: 'input-default'
    .main-form
      ２．本人確認
      .form-group
        %p.l-single-text
          安心・安全にご利用いただくために、
          %br
          お客さまの本人情報の登録にご協力ください。
          %br
          他のお客さまに公開されることはありません。
      .form-group
        %div
          = f.label :name, "お名前(全角)"
          %span.form-require
            必須
          = f.text_field :last_name, placeholder: "例) 山田", class: "input-default half"
          = f.text_field :first_name, placeholder: "例) 太郎", class: "input-default half"

      .form-group
        %div
          = f.label :name_kana, "お名前(カナ)"
          %span.form-require
            必須
          = f.text_field :last_name_kana, placeholder: '例) ヤマダ', class: 'input-default half'
          = f.text_field :first_name_kana, placeholder: '例) タロウ', class: 'input-default half'
      .form-group
        %div
          = f.label :birthday, "生年月日"
          %span.form-require 必須
          .birthday-select-wrap
            = raw sprintf(f.date_select(:birthday, with_css_classes:'XXXXX', prompt:"--",use_month_numbers:true, start_year:Time.now.year, end_year:1900, date_separator:'%s'),'年','月')+'日'
    
    .main-form
      ３．発送元・お届け先住所入力
      .form-group
        %div
          = f.label :name, "お名前(全角)"
          %span.form-require
            必須
          = f.text_field :last_name, placeholder: "例) 山田", class: "input-default half"
          = f.text_field :first_name, placeholder: "例) 太郎", class: "input-default half"

      .form-group
        %div
          = f.label :name_kana, "お名前(カナ)"
          %span.form-require
            必須
          = f.text_field :last_name_kana, placeholder: '例) ヤマダ', class: 'input-default half'
          = f.text_field :first_name_kana, placeholder: '例) タロウ', class: 'input-default half'
      
      .form-group
        = f.label :postal_code, "郵便番号"
        %span.form-require
          必須
        = f.text_field :postal_code, placeholder: "例）123-4567", class:"zip-code-form"
      .form-group
        = f.label :prefecture, "都道府県"
        %span.form-require
          必須
        = f.email_field :prefecture, placeholder: '東京都', class: 'input-default'
      .form-group
        = f.label :municipalities, "市区町村"
        %span.form-require
          必須
        = f.text_field :municipalities, placeholder: "例）中央区銀座", class:"input-default"
      .form-group 
        = f.label :block_number, "番地"
        %span.form-require
          必須
        = f.text_field :block_number, placeholder: "例）1-10-10", class:"input-default"
      .form-group 
        = f.label :building_name, "建物名"
        %span.form-require-not
          任意
        = f.text_field :building_name, placeholder: "例）GINZAビル", class:"input-default"
      .form-group 
        = f.label :room_number, "部屋番号"
        %span.form-require-not
          任意
        = f.text_field :room_number, placeholder: "例）201", class:"input-default"
      .form-group 
        = f.label :phone_number, "電話"
        %span.form-require-not
          任意
        = f.text_field :phone_number, placeholder: "例）080-1234-5678", class:"input-default"
      
      .form-group-1
        %p.form-group__text--center
          「登録する」のボタンを押すことにより、
          = link_to "利用規約", "#" , target:"_blank"
          に
          %br
          同意したものとみなします       
        = f.submit '登録する', class: "submit-form", url: "address_path"



= render "registration_footer"
